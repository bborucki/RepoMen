CFLAGS := -g
targets=client
src = client.c
objs = $(patsubst %.c,%.o,$(src))
libs = -L../lib -net

all: $(targets)
.PHONY: all

client: $(objs) ../lib/net.h
	gcc $(CFLAGS) -o client $(objs) $(libs)

$(objs) : $(src)

clean:
	rm $(objs) $(targets)